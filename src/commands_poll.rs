extern crate serenity;

use serenity::prelude::*;
use serenity::model::gateway::Ready;
use serenity::model::channel::Message;
use std::io::*;
use std::path::Path;

pub fn c_poll(ctx: Context, msg: Message, args: Vec<&str>) {

    pub fn create() {
        
    }

    pub fn remove() {
        
    }

}
